 // if(arr[pivot] >= key && key <= arr[size-1])
    // {
    //  cout<< "the key element present in " << binary << " index " << endl;

    //     /* code */
    // }
    // else if(key >= arr[pivot] && key >= arr[0])

    // {
    //    cout << "the key element present in " << binary << " index " << endl; 

    // }
    // else
    // {
    //     cout << "element is not present in array " << endl;
    // }